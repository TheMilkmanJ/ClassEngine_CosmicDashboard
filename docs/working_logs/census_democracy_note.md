# The democratic count — working note (2026-07-27)

> **Banner (2026-08-05):** The “licensed by the blindness principle” grade below is **withdrawn**
> (docket **#126** / `PRTOE_honest_status.md`). **No single criterion returns 9/10.** Present
> standing: c = 9/10 is a **counting assumption the data checks**, not a derived licence. This file
> is a **historical working note**, not a living derivation of c.

Task #6 working note; companion script `scripts/census_democratic_license.py`.

The open step was one clause: c = 9/10 requires an equal share per channel,
"which the framework does not force." The licensing argument, at argument grade
with its condition named:

- The recorded census principle is blindness — the medium couples to energy
  content, not particle identity.
- Weightings therefore diagnose sectors: a share taken in the MEDIUM sector is
  identity-blind and forces the democratic count (9/10); a share taken in the
  ELECTROMAGNETIC sector would weight by charge² (8/9); roster variants give
  12/13-class counts.
- The amplitude chain (ε = c·f̄·α_c) is built from the medium's coupling and the
  winding average — the medium sector. So equal share follows from the recorded
  principle, CONDITIONAL on that sector assignment, which is named and
  falsifiable rather than hidden.

Referee arithmetic (exact): current ensemble width on c ≈ 4%; separating 9/10
from 8/9 at 3σ needs σ_ε ≈ 0.4%; 12/13 falls first at ~0.9%. The running α_c/ε
chains are the instrument.

Observation at weight zero: this is the third appearance of one counting law —
equal integer occupancy per cell gives the vacuum energy (recorded), the
lepton-ring balance (the occupancy lock, candidate), and the equal channel
share (this note). One principle, three uses; noted, never stacked as evidence.

Grade: the count moves from "assumption the data confirms" to "licensed by the
blindness principle with a named sector condition." Not derived; the license
inherits the principle's own argument grade.
