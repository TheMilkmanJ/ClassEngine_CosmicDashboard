# PROMOTE_CANDIDATES — leftover sweep 2026-08-04

**Source:** `docs/working_logs/_runs/desk_compute_full_20260804/leftover/`  
**Fence:** do **not** edit living shelf claims without **dual evidence**.  
**Rule:** exit 0 ≠ PASS; token PASS ≠ COMPLETE.

This wave found **zero COMPLETE promotions**. Below are **card-only** (or lower) notes if living docs later want a cite to a recompute log.

---

## COMPLETE / shelf

| id | grade | note |
|---|---|---|
| — | **none** | No dual-evidence COMPLETE. Do not stamp residual-OPEN items COMPLETE. |

---

## Card-only (optional cite; not shelf COMPLETE)

### 1. `g_over_eps_is_the_roster`

| field | value |
|---|---|
| Log | `leftover/logs/g_over_eps_is_the_roster.log` |
| Claim (arithmetic) | `g/ε = N` with `N = 10` = roster size (nine charged + vacuum seat); same 10 as in `c = 9/10` |
| Controls | script reports **ALL CONTROLS PASS**; N=9 and N=11 miss recorded `g = 54 α/π` |
| Residual OPEN | closed form 0.12543 vs minimiser fit 0.120 (**+4.5%**); *why S8 sees total while m_e shift sees average* still owed |
| **Grade recommendation** | **desk audit / arithmetic card only** — not Derived COMPLETE; residual firewall stands |

### 2. `dark_colour_uniqueness_proof` (+ `dark_count_uniqueness`)

| field | value |
|---|---|
| Logs | `leftover/logs/dark_colour_uniqueness_proof.log`, `dark_count_uniqueness.log` |
| Claim | Self-cancellation ⇒ `N_f` integer only for `N_c \| 2`; nontrivial solution **SU(2), N_f=3** (N_c=1 empty) |
| Warrant upgrade | finite scan → divisibility one-liner; count uniqueness conditional on fundamental-rep + vacuum-cap bookkeeping |
| Residual OPEN | lattice campaign still referee; adjoint-flavour reading would void; **not derived** from Lagrangian |
| **Grade recommendation** | **licensed candidate / card** — strengthens recorded uniqueness; **not** COMPLETE derivation |

### 3. `pi_over_12_is_the_zero_crossing`

| field | value |
|---|---|
| Log | `leftover/logs/pi_over_12_is_the_zero_crossing.log` |
| Claim | `π/12 = arccos(−1/A) − 2π/3` at Koide `A = √2` (exact ~1e-16); unique A producing it |
| Implication | withdraws “second kind of object for π/12”; same Koide object as Q via `A² = 6(Q−1/3)` |
| Residual OPEN | **why** neutral phase is `φ_e + φ_cross` (displacement rule) still input |
| **Grade recommendation** | **identity card / framing correction** — good cite for angle identity; does **not** close #81 fully |

### 4. `census_scaling_network` (self-check only)

| field | value |
|---|---|
| Log | `leftover/logs/census_scaling_network.log` |
| Claim | VOS attractor exhibits `ξ/l_H` fixed point; self-check `k_*ξ ≈ 3.45e-3`, frozen `n_s=4`, scaling `n_s=1` |
| Residual OPEN | exact `γ*` from PRTOE medium Lagrangian; residual red tilt source uncomputed |
| **Grade recommendation** | **desk audit / exhibit** — mechanism class shown; **value still OPEN**; do not promote as COMPLETE cosmology |

---

## Explicit NON-candidates (token noise or negatives)

| label | why not promote |
|---|---|
| `winding_quadratic_pricing` | token PASS is internal series check; verdict **#55 derivation NOT AVAILABLE** |
| `occupancy_lock_cannot_deliver` | token FAIL; hardens delivery-law contradiction — debt re-point, not a win |
| `as_coincidence_price` | **PRICED AND HELD**, not promoted |
| `beta_holders_elimination` | structured **negative** |
| `biposh_estimator_pass`, `kapitza_junction_response` | **timeout** @ 120s — ungraded |
| `de_value_alpha2_handshake` | in-log “DERIVED” language is single-script; no dual evidence → **not** shelf COMPLETE this wave |
| All other exit0 leftovers | residual-named desk audits only |

---

## Action

1. Keep cards as **working-log cites** if useful.  
2. **Do not** promote living shelf / COMPLETE without a second independent line of evidence.  
3. Timeouts (`biposh_*`, `kapitza_junction_response`) may be re-run later with timeout 300s if CLASS-free but CPU-heavy.

*NO FABRICATIONS. Card-only list. COMPLETE promotions this wave: 0.*
