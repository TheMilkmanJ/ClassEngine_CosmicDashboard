# CONSTRUCTION_SS — T-S4 unitarized σσ at λ ~ 45.7

**Package:** `docs/working_logs/_runs/theory_construction_wave_20260805/side/`  
**Card:** **T-S4** (σσ scattering amplitude — desk-class ρ_Λ precision residual)  
**Date:** 2026-08-05  
**Rule:** NO FABRICATIONS. Leave MCMCs. No PolyChord. **Do not invent a scattering amplitude.**  
**COMPLETE:** **0**

---

## Grade

| field | value |
|---|---|
| **GRADE** | **MISSING_INPUT** |
| Existence claim ρ_Λ¼ | **stands** (existence, not precision) |
| Precision two-decimals / point a₀ | **blocked** on unitarized / reliable amp |
| Free precision dial | **KILL** |
| Lands this construction | **0** |

---

## Authority (stocked)

| surface | role |
|---|---|
| `docs/PRTOE_cosmological_constant.md` banner; `:30–31`; `:402–406`; `:727–753`; `:747–750`; claims ledger #6 `:801` | residual demand + tree table + MISSING_INPUT currency |
| `docs/PRTOE_DEPENDENCY_TREE.md:68` (ρ_Λ row) | existence not precision; residual = scattering amplitude desk question |
| `docs/working_logs/_runs/theory_exhaust_20260805/desk/shelf_desk_exhaust/` | T-S4 exhaust stamp (MISSING_INPUT) |
| `scripts/lhy_control_edge_refuted.py` | LHY demotion-reason / gate openability — **not** the amplitude |

---

## 1. What unitarized σσ at λ ~ 45.7 would require

Shelf residual (`PRTOE_cosmological_constant.md:747–750`):

> Remaining blocker: **is tree-level σσ scattering reliable at λ = 45.7?**  
> Tools named: chiral perturbation theory and **unitarised amplitudes** (incl. f₀(500) literature already cited for the σ mass).  
> **Currency:** unitarized σσ at λ ~ 45.7 = **MISSING_INPUT** — calculation not stocked; do not invent an amplitude.

### Construction demand (schema — not a derivation)

A successful T-S4 close must deliver **at least one** of:

| # | deliverable | why required |
|---|---|---|
| S1 | Threshold 2→2 **σσ** amplitude (or equivalent a₀) at sector coupling **λ ≈ 45.7** under a **named, unitarised / resummed** framework — not tree contact alone | Tree at λ=45.7 “not quantitatively reliable” (`:785–787`); contact vs contact+σ-exchange already moves a₀ by ~6× (`:730–737`) |
| S2 | Documented map of that amp into **a₀ in units of interaction range**, then into the stocked one-way bound `correction on ρ_Λ¼ = 1.345%/(Λa₀)` (`:758–761`) | Precision language is the bound + point placement, not a free % dial |
| S3 | Reliability statement at this coupling (unitarisation / ChPT control; s-channel pole kinematics at s=4m_σ² noted on shelf `:743–745`) | Without it, tree table stays estimate-only |
| S4 | Explicit **non-claim** of inventing a₀ to force two-decimal agreement with +0.44% gap | Kill free precision dial |

**Convention already pinned on shelf** (`:724–725`): V = (λ/4)(φ² − f²)² → quartic (λ/4)σ⁴ **and** cubic λfσ³; σ-exchange is **not optional**.

**Tree bookkeeping (estimates only — not the residual close):**

| reading | amplitude | on ρ_Λ¼ |
|---|---|---|
| contact only | −6λ = −274 | **0.59%** |
| contact + σ-exchange (s,t,u) | −36λ = −1645 | **0.10%** |

Both favourable *if* tree were trusted; spread **is** the question. Hard-sphere ceiling of one-way bound: **0.90%** on ρ_Λ¼ (`:770`, `:781–783`).

### What would unstick (honest)

- Desk **computation** of unitarised threshold amp at λ~45.7, **or**
- Literature a₀/(range) with **documented** map into 1.345%/(Λa₀) — **not invented**,  
- **or** keep existence-only grade and **never** advertise two-decimal precision.

Lattice P-048 (T_c/√σ) gates **τ**, not this amplitude (`DEPENDENCY_TREE:68`; shelf separates lattice half from desk radiative band).

---

## 2. Stocked formula / script partials

| artifact | what it is | is it the unitarized σσ amp? |
|---|---|---|
| Tree table `:730–733` | contact −6λ vs full −36λ bookkeeping at λ=45.7 | **No** — tree estimates; reliability unpaid |
| One-way bound `1.345%/(Λa₀)` | machine-backed band 0.10–0.90% once a₀ known | Bound only; needs a₀ |
| λ = 4π²/(N_c F) → **45.7** at F≈0.4316 | convention / coupling identity (`:720–722`) | Input to amp problem, not amp |
| `scripts/lhy_control_edge_refuted.py` | LHY truncation vs QMC literature; “gate openable at desk” | **No — not the amp** (explicit non-identity) |
| QMC-minus-LHY residual at na³ | control-edge follow-on error budget | Related to ρ_Λ error; **≠** unitarized σσ |

### Hunt under `scripts/`

No stocked unitarized / ChPT σσ threshold amplitude script at λ~45.7:

- No `*ss_amp*`, `*unitar*σσ*`, `*sigma_sigma*` compute path that returns the residual number.  
- `lhy_control_edge_refuted.py` reconfirm = gate/LHY object only.

**Verdict:** **no compute path** to a T-S4 number under stocked corpus.

---

## 3. MISSING_INPUT + construction schema only

Because S1–S3 are not stocked as runnable compute:

```
STATUS: MISSING_INPUT
OBJECT: unitarized (or otherwise reliability-controlled) σσ threshold 2→2 amplitude
        at λ ~ 45.7, or equivalent a₀ / range with map into 1.345%/(Λa₀)
BLOCKER: calculation not attempted / not stocked (CC :747–750)
EXISTS: existence ρ_Λ¼ chain (conditional) stands
FORBIDDEN: invent amp; claim two-decimal precision without amp; treat LHY script as amp close
```

**Construction schema (work program — not a land):**

1. Fix potential convention already on shelf (λ/4 quartic + cubic).  
2. Choose named unitarisation / ChPT tool class (shelf names tools; does **not** pick a result).  
3. Compute threshold T-matrix / a₀ at λ=45.7 with control estimates.  
4. Map to Λa₀ and ρ_Λ¼ correction; compare to +0.44% existence gap **without** tuning.  
5. Grade: point precision **only if** reliability holds; else keep existence + bound.

---

## 4. Kill free precision dial

| claim | status |
|---|---|
| Invent a₀ / amp to “finish” two decimals | **KILL** |
| Precision ρ_Λ from τ-chain alone | **KILL** (existence-only; shelf banner) |
| Tree table as measured / unitarised amp | **false** |
| LHY control-edge exit0 closes T-S4 | **false** |
| Silent drop of residual while advertising precision | **KILL** |
| λ gate = lattice campaign alone for radiative band | shelf separates τ (lattice) from desk amp |

---

## 5. One-liner

> **T-S4 MISSING_INPUT:** unitarized σσ at λ~45.7 not stocked; tree partials and LHY control-edge are **not** the amp; free precision dial **killed**; existence claim stands; COMPLETE **0**.

*NO FABRICATIONS. No invented amplitude.*
