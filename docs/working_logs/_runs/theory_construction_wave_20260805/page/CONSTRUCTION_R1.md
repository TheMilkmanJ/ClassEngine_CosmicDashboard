# CONSTRUCTION_R1 — Dump / occupation-transfer candidates (parent L1 / residual R1)

**Package:** `docs/working_logs/_runs/theory_construction_wave_20260805/page/`  
**Date:** 2026-08-05  
**Scope:** R1 only — candidate **classes** of licensed dump / occupation-transfer laws that *could* be admitted for future write-once scoring  
**Priors:** `desk_t6_page_micro_20260804/R1_R2_R5.md` · `page_t8_residual_demand` R1 · `page_d4_microphysics` D4 map · `page_t8/CONSTRUCTION_LEVERS` L1 · `DEAD_DENIFY` densify kill · F1 S⋆ fence  
**Champion residual (arrays reconfirm this package):** range/\(S_\star\) = **0.113154…** on bin **[0.10, 0.11)** (need ≤ **0.10**)  
**Fences:** NO FABRICATIONS · `page_curve_claimed: false` · no densify thrash · no coevolve production · leave MCMCs · no PolyChord · pure \(S_\star\) inflation **DEAD** (F1)  
**Stocked licensed dump law text:** **none** → every candidate below remains **MISSING_INPUT** until named operator + theory schedule land in corpus

---

## 0. Residual target (R1-specific)

R1 is the **dump / occupation-transfer** DOF class. Instrument separates **entangling (TMS)** from **dump (BS)**. Fail window is early TMS+BS overlap (\(f\sim0.057\)–\(0.072\); frames 43–54; monotone \(S\uparrow,u\uparrow\)).

| quantity | v13 value |
|---|---|
| Fail bin | **[0.10, 0.11)** |
| range (numerator) | **0.0018883423986319587** |
| \(S_\star\) (denominator) | **0.016688199517780646** |
| range/\(S_\star\) | **0.11315435176934464** |
| Need | ≤ **0.10** |

**What R1 must eventually do (if ever licensed):** raise early \(\mathrm{d}u\) relative to \(\mathrm{d}S\) through \(\Delta u=0.01\) at \(u\sim0.10\)–\(0.11\) **jointly** with T2+stall+DC3 — not pure \(G_{\mathrm{BS}}\) retune, not densify, not pure \(S_\star\) inflation (F1).

**Champion instrument dump pins (stocked schedule; not a new law):**

| pin | v13 value | role |
|---|---|---|
| `G_BS` | 4.4 | dump amplitude scale |
| `BS_MILD` | 0.205 | early dump floor |
| `BS_RAMP_POWER` | 1.6 | dump ramp shape |
| `BS_START` | 0.0 | dump window start |
| late EXTRA_BS | sweeps start 0.42; 11 sweeps; G frac 1.1 | late reach / purification — **not** early-bin residual handle |

---

## 1. Schema statement (unchanged; not derived)

> Core→radiation quanta transfer is **not** exhausted by present BS envelope pins. A **named microphysical dump law** (operator-level occupation channel with fixed, theory-justified schedule — not free `BS_MILD`) raises early \(\mathrm{d}u\) relative to \(\mathrm{d}S\) inside \([0.10,0.11)\) so range/\(S_\star\le0.10\), while completing unit-weight reach and freeze without late multivalued \(S\).

---

## 2. REQUIRED_INPUTS (must fill before implement/score as new dynamics)

| # | Input | Meaning | Stocked? |
|---|---|---|---|
| R1-I1 | **Named dump operator** \(\mathcal{O}_{\mathrm{dump}}\) | Operator-level core→rad occupation channel (not a header alias) | **MISSING_INPUT** |
| R1-I2 | **Theory-justified schedule** \(w_{\mathrm{dump}}(f)\) or equivalent | Fixed from micro content; not free `BS_MILD` / `G_BS` / EXTRA_BS grid | **MISSING_INPUT** |
| R1-I3 | **Channel structure** | Greybody-weighted / unitary channel map compatible with N1–N4 | **MISSING_INPUT** (grammar exists; law does not) |
| R1-I4 | **Independence certificate** | Law not fitted to early-bin 0.10; not pure \(G_{\mathrm{TMS}}\) dual | **MISSING_INPUT** |
| R1-I5 | **Write-once implementation plan** | New `coevolve_v{N}.json` only after I1–I4; never overwrite v13 | **not started** (no law) |
| R1-I6 | **F1 disclosure plan** | Future score must report early-bin range **and** \(S_\star\) vs v13 (class A/B only) | **protocol ON** (see [`F1_BIND.md`](F1_BIND.md)); no artifact yet |

**Instrument already stocked (not substitutes for I1–I5):** BS envelope pins on champion; greybody BS channel grammar in coevolve instrument; champion history under stocked BS (T8 still fails).

---

## 3. Candidate dump / occupation-transfer law classes (construction dump)

These are **CANDIDATE classes** for kill-seeking scrutiny. They are **not** Derived laws, **not** implemented, **not** claimed to clear T8. Each must still fill REQUIRED_INPUTS before any production coevolve.

### R1-C1 — Greybody-fixed core→rad occupation channel (licensed reweight)

| field | content |
|---|---|
| **Idea** | Dump rates fixed by theory-derived \(\Gamma_j(\omega)\) / surface-gravity / barrier map, not free `BS_MILD`·\(\sqrt{\Gamma_j}\) product under independent \(G_{\mathrm{BS}}\). |
| **Why it might act on early \(\mathrm{d}u/\mathrm{d}S\)** | Fail is overlap ratio; reweighting early-mode dump can advance \(v\) faster per unit TMS squeeze **if** weights are not free. |
| **can-exist** | Instrument already multiplies dump by \(\sqrt{\Gamma_j}\); greybody language is stocked grammar. |
| **should-not-exist** | Hand densify of midband fracs sold as “greybody law” (D3/R6 densify kill). Free barrier params fitted to [0.10,0.11). |
| **densify kill** | **ON** — any mode-count / midband campaign without continuum/barrier **derivation** is DEAD. |
| **S⋆-only kill (F1)** | Channel that only boosts late peak \(S_\star\) / EXTRA_BS cosmetics while early range stays ≈0.001888 → **DEAD**. |
| **Grade** | **SURVIVOR-SCHEMA · MISSING_INPUT** (no derived \(\Gamma_j\) law text) |

### R1-C2 — Unitary multi-level occupation transfer (structured dump Hamiltonian)

| field | content |
|---|---|
| **Idea** | Named interaction Hamiltonian \(H_{\mathrm{dump}}\) on core⊗rad Fock space with O(1) theory-fixed couplings; continuous mild transfer not exhausted by single scalar \(G_{\mathrm{BS}}\). |
| **Why it might act** | Changes occupation→energy map so \(u\) advances differently under same entangling drive (related to L3 free-H class but dump-side). |
| **can-exist** | Coevolve already has free matrix \(A\) + BS blocks; alternate dump structure is a legitimate DOF **class**. |
| **should-not-exist** | Free multi-level gaps dialed to early bin; non-unitary “occupation inject” that fails N1–N3. |
| **densify kill** | Adding modes to “need more levels” without spectrum derivation = densify thrash **DEAD**. |
| **S⋆-only kill (F1)** | Late-only dump boost inflating peak while early \(\Delta S\) fixed → **DEAD**. |
| **Grade** | **SURVIVOR-SCHEMA · MISSING_INPUT** |

### R1-C3 — Horizon-production-seeded mild dump (occupation present before strong TMS)

| field | content |
|---|---|
| **Idea** | Licensed early seed of rad occupation from continuum/horizon production so mild dump can advance \(u\) **before** strong TMS (R7-adjacent; dump-side of D1 lesson). |
| **Why it might act** | D1 pure-BS from vacuum died; lawful seed could separate “occupation present” from “entanglement large.” |
| **can-exist** | Horizon/flow production is already instrument narrative; seed law is a real missing handle class. |
| **should-not-exist** | Manual `nbar` dial; thermal seed killing N1–N2; hidden \(G_{\mathrm{TMS}}\) during “seed phase”; D1 PHASE rename. |
| **densify kill** | Seeding extra midband modes by hand = densify **DEAD**. |
| **S⋆-only kill (F1)** | Seed that only loads late entropy / peak height without cutting early range → **DEAD**. |
| **Grade** | **FRAGILE-SCHEMA · MISSING_INPUT** (thin ice next to D1; requires independent seed micro text) |

### R1-C4 — Continuous mild dump schedule fixed by sonic-horizon flux (analog Hawking rate law)

| field | content |
|---|---|
| **Idea** | Replace free \(w_{\mathrm{bs}}(f)\) envelope by a flux schedule derived from finite sonic-horizon / healing-length microphysics (registered Page object in scaffold corpus). |
| **Why it might act** | Early \(\mathrm{d}u\) becomes theory-fixed rather than `BS_MILD` thrash surface. |
| **can-exist** | Scaffold + week-N instruments already speak sonic-horizon / greybody; schedule as **suggestion** under exploratory protocol. |
| **should-not-exist** | Drawing \(w_{\mathrm{bs}}(f)\) from cartoon Page \(4v(1-v)\) (scaffold toy is **NOT a result**). Fitting flux params to 0.10. |
| **densify kill** | Flux “needs denser spectrum” without derivation → densify DEAD. |
| **S⋆-only kill (F1)** | Flux law scored only via larger \(S_\star\) → DEAD. |
| **Grade** | **SURVIVOR-SCHEMA · MISSING_INPUT** (no closed licensed flux→dump schedule text for T8) |

### Explicitly **not** candidates (killed densify / thrash)

| dead form | why dead |
|---|---|
| Header `BS_MILD` / `G_BS` / `BS_RAMP_POWER` grid | FORBIDDEN thrash (post-v13 sticky / joint fail) |
| Late EXTRA_BS boost as early-bin fix | Wrong window; late multivalued rebound |
| Mode densify / midband retune “for dump” | D3 EXHAUSTED → thrash; densify DEAD under D4 |
| Empty rename of BS envelope as “dump law” | Honesty kill |
| Pure \(S_\star\) inflation via late dump cosmetics | **F1 DEAD** |

---

## 4. can-exist (framework / instrument permission)

- Instrument separates entangling (TMS) from dump (BS); residual is early **ratio** of slopes.  
- Fail window is early TMS+BS overlap; dump that advances \(v\) faster per unit squeeze is the right DOF **class**.  
- D1 showed early T8 *can* flatten when early TMS is suppressed — a better dump might recover reach without two-phase thrash.  
- Corpus already speaks greybody-weighted BS — a **licensed** reweight is not alien grammar.  
- REQUIRED_INPUTS map is explicit; construction is gateable.

---

## 5. should-not-exist (adversarial; includes S⋆-only)

- **Empty rename:** “new dump law” with no operator content = `BS_MILD` under a hat.  
- **Steerability:** free dump amplitude dialed until T8 greens, cited as “micro.”  
- **Late multivalued rebound:** extra early dump → late EXTRA_BS / freeze games → stall-class T8 fails.  
- **Null risk:** stronger dump violating N1–N3 / thermal N2 if not unitary channel structure.  
- **Densify thrash:** mode-count densify sold as dump microphysics — **DEAD** (D3 / DEAD_DENIFY).  
- **S⋆-only lever (F1):** a “dump” construction that **only** raises global Page peak \(S_\star\) (late/peak window) while leaving early-bin absolute range fixed, so range/\(S_\star\) falls by denominator alone. Peak region (\(u^*\approx0.267\)) is **disjoint** from fail bin \([0.10,0.11)\). Pure \(S_\star\) inflation is **DEAD** as T8 pass ([`F1_BIND.md`](F1_BIND.md)).

---

## 6. joint-gate risk

| gate | risk |
|---|---|
| T2 | Under-dump → \(u_{\mathrm{late}}<0.9\) (D1 class) |
| stall | Over-dump late → long freeze stalls |
| DC3 | Dump through weight-borne core energy → frozen-weight fail |
| T8 late | Dump boosts historically reintroduce multivalued tails |
| N1–N4 | Non-unitary / thermal-breaking dump |

---

## 7. kill table

| Kill | Trigger |
|---|---|
| Thrash kill | Only header `BS_MILD` / `G_BS` / EXTRA_BS grid |
| Densify kill | Mode-count / midband campaign without licensed continuum or dump derivation |
| Honesty kill | Free dump constant dialed to 0.10 without independent license text |
| Joint kill | Early T8 green with T2/stall/DC3 fail |
| Null kill | N1–N4 fail under new channel |
| **F1 / S⋆ kill** | T8_pass claim with early-bin range **unchanged** (vs v13) and only \(S_\star\) up |

---

## 8. Stocked vs MISSING_INPUT (this wave)

| Asset | Status |
|---|---|
| BS envelope pins on champion | Stocked **instrument** (not a new law) |
| Greybody BS channel grammar | Stocked **instrument** |
| Named microphysical dump operator + theory schedule | **MISSING_INPUT** |
| Derived core→rad transfer law text | **MISSING_INPUT** |
| Write-once coevolve under new dump law | **not started** (forbidden until law text) |
| Coevolve production this package | **0** |
| Densify runs this package | **0** |

**Immediate grade:** **SURVIVOR-SCHEMA · MISSING_INPUT · no production**  
**Land this package:** **0**  
**CANDIDATE packet:** **0** (T8 still fails; no law)

---

## 9. Explicit non-claims

- Candidate classes ≠ Derived dump law.  
- Listing C1–C4 ≠ prediction that any clears T8.  
- REQUIRED_INPUTS filled later ≠ automatic joint land.  
- No invented operator claims T8 pass without instrument.  
- `page_curve_claimed` remains **false**.

---

*End CONSTRUCTION_R1.md. NO FABRICATIONS. Densify thrash DEAD. F1 ON.*
