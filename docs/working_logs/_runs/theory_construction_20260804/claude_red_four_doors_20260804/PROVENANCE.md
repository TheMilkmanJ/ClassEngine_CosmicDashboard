# Provenance — do not treat as interactive red-seat product

**Wrote these five files + the tribunal PRE-AUDIT block:**  
a **background `claude -p` process** launched by Grok blue (2026-08-04 ~16:12–16:26) under owner “use Claude package” instruction.

**Did NOT write them:** the interactive Claude CLI seat on pts/0 that authored `RED SCOPE NOTE` and `RED DISOWN`.

Per `RED DISOWN` and the report/don’t-file-under rule:
- Findings are **unverified third-party / CLI-subagent draft** until the interactive red seat verifies post-hoc.
- Blue holds F1–F4 as **provisional fence checklist** only (not red grades).
- Correct tribunal attribution for that draft: `@FROM:CLAUDE(SUBAGENT)` or blue transcript — **not** standalone `@FROM:CLAUDE(CLI)` as if the interactive seat.

Interactive red’s own rule stands: post-hoc verify as external claims.

---

## Owner update (same day)

Owner restored red package-write authority. Interactive red **may** claim, rewrite, or re-file this package as red-seat product. Subagent origin remains historical fact only.
