> **PROVENANCE:** background `claude -p` subagent (blue-launched), **not** interactive red seat. See `PROVENANCE.md`. Unverified until interactive red post-hoc.

# RED PRE-AUDIT — N6 kill-RP-A disposition (must NOT fire from absence)

**Seat:** Claude (red) · **Date:** 2026-08-04 · **Mode:** pre-audit, fences only
**Target package:** `n6_kill_rpa_20260804/` — **directory exists, empty at write time**
**Instrument:** none stocked. Nearest prior: `scripts/bounce_rpA_scaffold.py`
**Priors read:** `n1_fa2/SURVIVORS.md` · `n2_match_book/SURVIVORS.md` · `n3_theta_3d/SURVIVORS.md` · `fa3_metric_off/KILL_AND_FALSIFIERS.md` · `page_t8_residual_demand/SURVIVORS.md`

---

## 0. A kill is a claim. Red grades it like one.

This is the door where the failure mode inverts and therefore the door red watches
hardest. Every other package is tempted toward a **soft-close** — calling a near-miss a
land. N6 is tempted toward its mirror image, a **soft-kill**: declaring the residual
impossible so it can leave the open board.

Both dispositions **empty the desk**. If N6 fires, a long column of OPEN-BLOCKED rows
converts to CLOSED-DEAD, the failures ledger fills, the residual disappears, and the
program looks finished. That the direction feels humble is exactly what makes it cheap.

> **Red's standing position: an unjustified kill and an unjustified land are the same
> offence.** RP-A is, on blue's own record, *"the only non-killed silhouette"*
> (`scripts/bounce_rpA_scaffold.py` header; `fa3_metric_off/CONSTRUCTION.md:13-16`).
> A kill of the last surviving silhouette is a **larger** claim than a land, not a
> smaller one — it asserts impossibility over all legal parts, quantified over
> instruments that do not exist.

---

## 1. Every stocked authority says N6 does not fire yet

Four independent survivor files, none of which red wrote:

| source | line | text |
|---|---|---|
| `n3_theta_3d/SURVIVORS.md` | 12 | N6 — *"open, not fired — **premature without impossibility proof**"* |
| `n3_theta_3d/SURVIVORS.md` | 32 | *"Optional N6 only with **proof-level obstruction**, not absence of formula alone"* |
| `n1_fa2/SURVIVORS.md` | 25 | *"Optional N6 kill-RP-A if S1–S3 **prove impossible without manufacturing**"* |
| `n2_match_book/SURVIVORS.md` | 48 | *"Optional N6 ... if **impossibility proved** without manufacturing"* |

And the correct posture is already written down elsewhere in the corpus, for a different
residual — `page_t8_residual_demand/SURVIVORS.md:99-101`:

> *"Absence of a joint land after D1–D3 is **not proof** no micro law exists. It **is**
> proof that header + densify + D1/D2 forms are the wrong program."*

**That K-class posture is the template.** N6's honest maximum is the second sentence:
*these routes are the wrong program.* It is not the first.

---

## 2. The stocked kill condition for RP-A is not the one N6 would be firing

`fa3_metric_off/KILL_AND_FALSIFIERS.md:23` gives the actual documented kill for the P2
gate:

> *"Medium stress **cannot** produce \(\langle\Theta\rangle>0\) under any legal GPE /
> averaging / 3D instrument (beyond toys) → gate \(\langle\Theta\rangle>0\) never fires
> → RP-A exterior turn dies."*

Blue's own data bears directly on this, and it points **away** from the kill:

| evidence | source | value |
|---|---|---|
| ⟨Θ⟩ turn under legal stress | `THETA_LOCK_HUNT.md:111` | **YES** (toy PAID) |
| `turn_paid_toy` | `logs/n3_theta_lock_scan.log` | **true** |
| max late ⟨Θ⟩ | log / `THETA_LOCK_HUNT.md:67` | **+1.8005** — positive |
| 1D GPE density turn | `THETA_LOCK_HUNT.md:88` | YES, all 6 cases |

The gate is a **sign** gate. \(\langle\Theta\rangle>0\) is observed. The sign gate fires.

The residual is **magnitude** — obstruction **C** — which is a different object, kept
separate throughout the corpus (`CONSTRUCTION.md:205`, `n1_fa2/SCORECARD.md:48`).

> **KILL-1 (the central one):** N6 must not fire the **sign** kill condition using
> **magnitude** evidence. "Late Θ never reaches 11.7" is not "⟨Θ⟩ never turns positive".
> Presenting the magnitude shortfall as satisfying `KILL_AND_FALSIFIERS.md:23` is a
> **category substitution** and red will grade it **DENIED**.

Note the honest residue: the stocked condition is quantified *"beyond toys"*, over
instruments that are **not stocked** (`INSTRUMENT_INVENTORY.md:31`, full-3D GPE
"**Not stocked**"). A universal quantified over non-existent instruments can be left
open; it cannot be **established**. That is not a technicality — it is the reason N6
cannot fire on this condition in either direction today.

---

## 3. Scan-based impossibility is invalid — four independent reasons

If N6 reaches for the N3 scans as its evidence base, all four of these apply at once:

1. **Finite grid over non-derived parameters.** κ, γ are *"toy reduced, not Derived GPE
   coefficients"* (`INSTRUMENT_INVENTORY.md:26`). A finite grid over free parameters
   bounds nothing, even inside the toy.
2. **The argmax sits on the boundary.** Prior best late row \((50,-5,3,0.05)\)
   (`THETA_LOCK_HUNT.md:74`) has three of four coordinates on the prior grid edges
   (`:38-41`); the best peak row \((50,-5,5,0.05)\) (`:77`) has four of four. **A scan
   whose maximum is at the wall does not bound its own box**, let alone the physics.
   The new instrument extends exactly those walls (`bounce_n3_gpe_late_theta.py:213-227`)
   — see `PRE_N3_GPE.md` K2.
3. **The object scanned is a stand-in, not the medium.** The 0D system is a *"reduced
   stress-channel ODE"* (`bounce_n3_gpe_late_theta.py:90`), a *"reduced stand-in"*
   (`INSTRUMENT_INVENTORY.md:12`). Bounding a stand-in bounds the stand-in.
4. **Max-over-scan is not a physical quantity.** It grows with row count
   (`PRE_N3_GPE.md` K1). A ceiling read off a max is a property of the sampling.

> **KILL-2:** any N6 impossibility argument resting on "the scans topped out at X" is
> **DENIED** on all four counts. Red will not accept a sweep as a proof.

---

## 4. What "manufacturing" would look like here — pre-registered

`n1_fa2/SURVIVORS.md:25` and `n2/SURVIVORS.md:48` both fence *"without manufacturing"*.
Red pre-commits to these as manufactured-kill signatures:

| signature | why it is manufacturing |
|---|---|
| Narrowing "legal parts" mid-argument until the survivors fall outside | Kill by redefinition |
| Treating **empty** as **impossible** | `n2/REPORT.md:57` — Israel is *"still empty"*. Empty ≠ impossible |
| Citing red's `PRE_N3_GPE.md` §2 domain question as impossibility content | Red asked a question and **explicitly declined to donate it**. See §5 |
| An `assert kill_fires` on a hardcoded boolean | Same vacuous-assert class as `PRE_ISRAEL.md` §0. `exit 0 ≠ PASS` — for kills too |
| "No route found after N packages" | Absence of proof. `page_t8_residual_demand/SURVIVORS.md:99` |
| Firing on obstruction **A** (already DEAD) to conclude RP-A dies | A killed the *continuous metric-ON* path; P1 exists precisely to escape it (`CONSTRUCTION.md:13-16`) |

---

## 5. Red will not supply the kill

`PRE_N3_GPE.md` §2 records a domain-of-validity question red raised about blue's own
target and instrument, **with its counter-evidence stated in the same breath**
(\(v_g/c_s = 2.1213\) at \(x=2\), unbounded at large \(x\) — a non-relativistic GPE has
no built-in \(c\)).

> **That question is not offered to N6 and must not be cited by N6 as impossibility
> content.** If red hands the kill package its missing argument and then grades the
> kill, red has graded its own work and the third seat is spent. Per red's board block
> of this morning: **that is a blue decision.** If blue wants that line, blue derives it,
> owns it, states its counter-evidence, and red grades it as blue's — with no credit
> for having been prompted.
>
> Red states this refusal on the board rather than declining silently, as committed.

---

## 6. Grade conditions when blue files TASK COMPLETE

| grade | conditions |
|---|---|
| **AGREE** | N6 **does not fire**. Disposition recorded as *open, not fired*. Package states the honest maximum in K-class form: *"these routes are the wrong program"* — naming which routes, with evidence. Sign-gate vs magnitude-obstruction kept explicitly separate. `bounce_closed` false, RP-A still the standing silhouette, 0 lands, COMPLETE 0 |
| **AGREE-IF** | Disposition correct but ≥1 of: sign/magnitude blurred in prose · "exhausted" and "impossible" used interchangeably · scan maxima quoted as ceilings without the boundary caveat · route-level deaths written without naming the routes |
| **DENIED** | Any of: **N6 fires** without a proof-level obstruction · magnitude shortfall presented as satisfying `KILL_AND_FALSIFIERS.md:23` · scan-based impossibility · "no formula found ⇒ no formula exists" · red's §2 question cited as kill content · `PRTOE_FAILURES_LEDGER.md` or any living doc written with an RP-A death **before** red AGREE · RP-A retired while obstruction C is merely OPEN |

**Red's prior:** N6 does not fire. Every stocked authority says so, blue's own data
falsifies the direction of the stocked sign-kill, and no impossibility theorem exists
anywhere on disk.

---

## 7. What a legitimate N6 firing would require (stated as a bar, not a route)

Red states the **bar** because the bar is a fence, and refuses the **route** because the
route is design:

1. A **proof-level obstruction** — a theorem or exact argument, not a sweep — that
   obstruction C cannot be closed by *any* legal parts, not merely by the parts tried.
2. The obstruction must be **stated as a proposition with its premises named**, so it can
   itself be attacked. A kill that cannot be falsified is not a kill.
3. Its premises must be **stocked or licensed**, not introduced for the purpose.
4. It must survive the observation that Israel/R3 is **empty, not excluded**
   (`n2/REPORT.md:57`).

Red does not say whether such an obstruction exists. Red says that nothing on disk
today is one.

---

## 8. Non-claims of this red note

- Red has **not** said RP-A is right, viable, or promotable.
- Red has **not** supplied, hinted at, or endorsed any impossibility argument.
- Red has **not** graded a package (`n6_kill_rpa_20260804/` is empty at write time).
- "N6 must not fire" is a statement about the **evidence**, not about the physics.

*NO FABRICATIONS. Pre-audit ≠ verdict. A kill is a claim.*
