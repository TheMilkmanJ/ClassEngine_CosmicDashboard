# RED cure — exit 0 ≠ PASS propagation (2026-08-04)

**Rule:** NO FABRICATIONS. exit 0 ≠ PASS. No physics invented. No MCMC / booking.

## Finding

Claude red: package working tables were already cured in places, but the **authority stamp** still bulk-labeled formulable recomputes as PASS.

## Verified already correct (no edit)

| file | status |
|---|---|
| `RECOMPUTES.md` | **3 PASS verdicts** / **6 desk audits**; FAIL 0 |
| `REPORT.md` | same counts; §2 table desk-audit vs PASS |
| `EDITS.md` | “3 PASS / 6 desk audits” |
| `WALL_TABLE.md` | desk audit / PASS verdict split on debt cells |

## Cured this pass

### 1. `docs/working_logs/_runs/THEORY_WALLS_QUEUE_20260803.md`

| locus | before | after |
|---|---|---|
| Metric row “Formulable recomputes parsed” | **9** (**PASS** / **0 FAIL**) | **9** logs exit 0 → **3 PASS verdicts** + **6 desk audits**; FAIL **0** |
| Recompute reconfirm table (9 rows) | all **PASS** | 3 **PASS verdict** + 6 **desk audit** exit 0 |
| Wall debt cells `koide_lock` / `rm_coherence` / `baryo_junction` | bulk **PASS** | desk audit (+ `tau_parseval` **PASS verdict** kept) |

### 2. `bounce_full_freeze_20260804/` (bounded soft-relabel)

Recompute table had **PASS EXIT 0** for logs that never emit a PASS grade token.

| class | n | label |
|---|---:|---|
| Log emits `GRADE: PASS (analytic)` | **1** | **PASS** (analytic) EXIT 0 — `bounce_bkl_stiff_check` |
| No PASS token (nogo confirm / desk) | **8** | **EXIT 0** (nogo confirm / desk) |

§2 paid-nogo **physics** grades unchanged (engines remain DEAD / PAID nogo). H_re stays **OPEN-BLOCKED**.

### 3. pass_label_hygiene package

Left intact (historical before/after rows). Not re-broken.

## Grep residual

No remaining authority-stamp **9 PASS** / **PASS / 0 FAIL** / **9/9 PASS** in:

- `open_theory_full_20260804/` (except intentional “not all PASS” / cure prose)
- `THEORY_WALLS_QUEUE_20260803.md` (now 3+6 language)

## Counts

| metric | n |
|---|---:|
| Primary authority stamp fixed | **1** (THEORY_WALLS FULL metrics + recompute table) |
| Bounce recompute soft-relabel | **1** package (REPORT + EDITS) |
| Physics closes invented | **0** |
| Status tags / MCMCs touched | **0** |

*NO FABRICATIONS. exit 0 ≠ PASS.*
