# RED AUDIT — theory task inventory (2026-08-04)

**Seat:** Claude RED (CLI). **Owner-authorized:** completeness · grade-honesty · duplicate/collapse.
**Scope fence:** audit lane only. **No mechanism proposed. 0 COMPLETE promotions. NO FABRICATIONS.**
**Target:** [`REPORT.md`](REPORT.md) · [`TASKS.md`](TASKS.md)

**Headline:** blue's answer to the owner — *is there more theory work: **YES*** — is **CONFIRMED**, and
**under-stated**. Nothing in this audit closes anything. Grade: **AGREE-IF** (three conditions, §4).

---

## 1. Completeness

Tiered by source authority. Tier 1 is inside blue's **own** declared authority list (REPORT §8) and is
the binding half; tiers 2–3 are scope calls for the owner.

### Tier 1 — missed inside blue's declared authorities

**C1 — three of nine named recompute residuals have no task card.**
`open_theory_full_20260804/RECOMPUTES.md` tables 9 logs with a "residual still OPEN" column, and
`THEORY_WALLS_QUEUE_20260803.md:73–85` restates it. Six map to cards; three do not:

| # | log | residual named OPEN | card |
|---|---|---|---|
| 3 | `supertrace.log` | absolute SI *G* | T-X3 ✓ |
| 4 | `koide_lock.log` | #101/#102 | T-W5 ✓ |
| **5** | `tau_parseval.log` | **`locking_without_Q`** | **none** |
| **6** | `fbar_lo.log` | **`c_w` underived** | **none** |
| **7** | `fbar_cw_lo.log` | **value of *a*** | **none** |
| 8 | `baryo_junction.log` | forward ω_J | T-W16 ✓ |
| 9 | `rm_coherence.log` | void floor | T-W6 ✓ |

`locking_without_Q` is not a package artifact — it is a live OPEN row on the shelf file itself:
`docs/PRTOE_koide_relation.md:743` ("Independent lock of τ without assuming Q=2/3"), repeated at
:13 and :732. It is a **distinct object** from T-W5, which owns row 4's #101/#102 + Wilson.

**C2 — `PRTOE_cosmic_magnetism.md` freezes two residuals; T-W6 owns one.**
`THEORY_WALLS_QUEUE_20260803.md:30` lists "Void floor ×20 (1.30 dex); **RM n_e amp**". T-W6's Object
is the 1.30 dex void shortfall only. The amplitude residual is carried OPEN on the shelf at
`docs/PRTOE_cosmic_magnetism.md:235` ("RM absolute amplitude | **OPEN** | needs external n_e model")
and :227. Unowned.

**C3 — naming conflict inherited by neither.** The same residual is "c₂ underived"
(`THEORY_WALLS_QUEUE_20260803.md:80`) and "c_w underived" (`RECOMPUTES.md` row 6). Because no card was
cut, the conflict is unresolved on the board.

### Tier 2 — `PRTOE_DEPENDENCY_TREE.md` (the file the owner named)

On the **literal** reading blue is complete: the four rows carrying an OPEN/ABSTENTION tag are the
2026-08-04 banner's, and all four are covered (live cosmology product → T-M1; Page Q6 → T-W15;
bounce H_re → T-W1; Strong CP → T-X4).

But the banner states in its own words that it **"does not regrade every cell"** (line 6), so the body
tiers stay live. Body-carried open objects with no card:

| object | tree | state as written |
|---|---|---|
| **docket #182** — re-key the ramp on the scalar's own 307–714 keV band; re-price BBN above 500 keV | Tier 1, line 48 | live docket (`PRTOE_DERIVATION_HUNT.md:655`; `_AUDIT_LEDGER.md:3648`; `_CANONICAL_VALUES.md:47`) — 53% of the band sits past the fence |
| **σσ scattering amplitude** (ρ_Λ precision leg) | Tier 3, line 68 | tree calls it **"a desk question"**; `PRTOE_cosmological_constant.md:402–403` — the 0.10–0.90% bound is "closed at the **desk** given the σσ amplitude rather than by the lattice" |
| **A_s lock-count C = 1 to ±22%** | Tier 3, line 66 | "the count and the non-running of α_c **stand or fall together**"; tree's own words: the corpus's highest-risk standing claim |
| **tenth-channel seat alignment** | Tier 5b, line 95 | "an **owed dynamical step**, not a written-in one" |
| **Koide transfer** — KMS pacing bound saturated; chirality sequencing | Tier 5b, line 94 | two candidate statements, distinct from #101/#102 and Wilson (census T6 agrees: "the twist transfer's pacing step; the sign-chain walk") |
| **task #98** — Galactic-Centre soliton exposure | Tier 1, line 47 | **authority conflict:** tree says *unpriced*; `_RESIDUAL_DEBT_CENSUS.md:77` says *priced, the sector's leading test* |

**The σσ item is the load-bearing one.** REPORT §F says "desk hygiene is **largely yes** exhausted —
further theory is construction / foundations, not free closes." A named desk-class computation sits
unlisted. That sentence needs a qualifier or that card needs cutting.

### Tier 3 — two standing boards not swept

REPORT §8 "Authorities" lists neither of these. Both are open-object registries:

- **`docs/working_logs/_E2E_DERIVATION_BOARD.md`** — Track A named-open residuals: **A1** κ ≈ 1 in
  c_chop = κ·d_⊥; **A2** approach OOM (gap −0.0028); **A3** value of *a* (ens [0.32, 1.36] vs fit
  1.80 @1.9σ); **A5** pour→release / first-principles n. Its Track B table (:43–44) carries *a* and
  κ as live residuals. *(A3 is the same object as C1 row 7 — it is open on two boards and carded on
  neither.)*
- **`docs/working_logs/_RESIDUAL_DEBT_CENSUS.md`** — its own "live open desk residue" list (:113–119):
  **T4** matched lensing fit (#32 / docket #161), **T5** power-spectrum route ungradeable + BipoSH
  data application, **T6** pacing + sign-chain, **T16** conditional axis-correlation, **T1** #98.

Not called an error — blue did not claim these as sources. Called a **scope gap**, because REPORT §A–F
reads as a whole-board inventory.

---

## 2. Grade-honesty

**G1 — T-X3: self-satisfying close, and the only §A/§B theory card with no Kill row.**
Close = "Named close **or permanent open stamp**". The task closes by writing "OPEN" on it. Absolute
SI *G* is unaffected either way, and with no Kill the card cannot detect its own failure. **Fix: add a
Kill; move "permanent open stamp" out of the Close column.**

**G2 — T-W3: the Close points at a do-not-reopen lane.**
Close = "Democratic derivation without fudge". `_E2E_DERIVATION_BOARD.md:50` lists **"c via
gravity-blindness democracy (#126)"** under **Do not reopen**; `PRTOE_DERIVATION_HUNT.md` §1 states the
licensing step "does not exist". The card sends desk work at a fenced lane. Its Kill ("fake derivation
from aesthetics") names no gate, while the tree (line 55) carries a quantitative one the card drops:
a 3σ call needs σ_c ≤ 0.0037 (≈10× sharpening, ≈100× sample); at present width 12/13 sits +0.53σ and
8/9 sits −0.38σ, so the ensemble separates nothing.

**G3 — T-W4: graded permanent and closable at once.**
Grade "permanent bet"; Close "IR referee / instrument". `_E2E_DERIVATION_BOARD.md` A4 rules:
"same-response base α **not derived** … value bet with A_s referee only. **Not open derivation debt.**"
T-X4 and T-X5 handle fenced items correctly with **Close = N/A**. T-W4 should match, or drop the
permanent-bet grade. Not both.

**G4 — demotion-as-close (family).** T-W2 "or claim demoted with ledger" · T-W5 "or residual retired
to ledger" · T-X3 "or permanent open stamp". Demotion **is** a legitimate disposition and an
improvement — `WALL_TABLE.md` §Improvement-path item 2 and item 3 say exactly that. It is **not** a
close of the physics object. As one column, COMPLETE counts can rise with zero physics.
**Recommend the card template split `CLOSE (physics)` from `DISPOSE (honesty)`.**
*(T-W1e's "or proof gap unclosable without manufacturing" is a genuine proof obligation — not in this family.)*

**G5 — T-W1g routes a physics close through red. Red declines the standing.**
Close = "Red AGREE on CANDIDATE note after positive-restatement hygiene". Red AGREE grades hygiene and
honesty; it cannot promote restored-arrow seating. The note is the artifact, not the object. Contrast
T-X6, where a red-AGREE close is correct **because the object is the audit**.

**G6 (minor) — T-X6 has no enumeration.** "Load-bearing red audits still `none`" with no list of which
board rows are load-bearing and currently unaudited. Never checkable complete. Ask blue for the list;
red will work it.

**Currency spot-check — T-M1 verified accurate.** lcdm R−1 **0.071122** @ N=21886 and dyad **0.072286**
@ N=21867 are the current last rows of `chains/cmp_lcdm_mnu_bbnfix.progress` and
`chains/dyad_mnu_bbnfix.progress`. **NOT bookable** stands; no `converged` flag on disk. Two notes:
(a) `RESIDUAL_OPEN.md:15` is two rows stale (0.059055 / 0.128943 @ N=20302) — the **inventory is the
more current document**, and RESIDUAL_OPEN should be refreshed to match, not the reverse;
(b) neither quotes rank count, and an unqualified R−1 is a standing house caveat.

---

## 3. Duplicates / collapse

**D1 — T-W1 ≡ ⋁(T-W1a … T-W1g); §E double-counts it.**
T-W1's Close is literally the disjunction of its children: "Legal F-A2 ρ_re *(=W1a)* + settled late Θ
*(=W1b)* or alternate match *(=W1c)* + (optional) force-branch *(=W1d)*; or proof-level N6 K1∨K2∨K3
*(=W1f)*". T-W1 holds no residual of its own. §E then counts "Canonical walls W1–W16 = **16**" **plus**
"Bounce sub-tasks T-W1a–g = **7**" as disjoint. **Fix: mark T-W1 a rollup and footnote §E.**

**D2 — T-W9 and T-W10 are riders, not walls.** Both classed "rides T-W1"; both grades are inherited
OPEN-BLOCKED-on-bounce. One wall's blockage is reported three times across three IDs. Independent
slivers are real and worth keeping — W9's seed law *where independent of bounce*, W10's
map-as-interpretation discipline + DE XOR — but the inherited half should be deleted, not restated.

**D3 — T-W11 + T-W12 collapse.** Same package (`WALL_TABLE.md:25` literally writes the file as
"same"), same state MISSING_INPUT, same close (hunt a licensed file:line), same kill (invent / guess).
One object, two line items.

**D4 — T-W13 + T-W14 collapse.** Both quantum wing, both "Desk DONE", both gated on the identical
owner foundations program, both fenced. **Blue's own §D queue item 9 already lumps W11/W12/W13/W14 as
one line** — the card granularity contradicts the queue in the same file.

**D5 — T-W16a ⊂ T-W16.** W16a's Close ("fill P2-1/P2-2 content **or fire K5**") is a strict subset of
W16's Close ("independent χ or J_seat or ω_J^micro; band score; **or K5 fire**"). K5 is a firing
condition in both: if K5 fires, two IDs close on one event.

**D6 (minor) — class inconsistency.** T-W7 is graded **OPEN-MACHINE** and filed in §A "Canonical walls",
while §C exists precisely to hold run-gated objects ("not theory, listed so inventory is complete").
Same class, two sections.

**D7 (minor) — null-object cards.** T-X4 ("Object: **None** (abstention)") and T-X5 ("Null stands; do
not invent") are ledger rows, not tasks. Correctly fenced; they inflate §E's T-X count of 6.

---

## 4. Verdict

**AGREE-IF.** Blue's headline is right and the discipline is right: **0** invented closes, most cards
correctly OPEN-BLOCKED / MISSING_INPUT, the "long is correct" posture is correct. The board is
**larger** than blue's ~25–30, not smaller.

**Lift conditions (Tier-1 only — all sourced inside blue's own declared authorities):**

1. **IF-1** — cut cards for the four unowned objects: `locking_without_Q`, `c_w`/`c₂`, value of *a*,
   RM n_e absolute amplitude. *(Or state on the board why a residual named OPEN in the authority
   carries no card.)*
2. **IF-2** — repair T-X3 (add Kill; "permanent open stamp" is not a Close) and T-W3 (the democratic
   route is on the do-not-reopen list — mark it, or delete the Close).
3. **IF-3** — footnote §E so T-W1 is not counted alongside T-W1a–g.

**Recommendations, not gates** (owner's scope call): tier-2 tree-body objects — **docket #182** and the
**σσ desk amplitude** first, since #182 is a live docket and σσ is explicitly desk-class; sweep the two
un-swept boards; the D1–D5 collapses; the CLOSE/DISPOSE column split.

**Red does not accept T-W1g's close condition** (G5).

*Audit only. No mechanism proposed. COMPLETE promotions from this audit: **0**. NO FABRICATIONS.*
