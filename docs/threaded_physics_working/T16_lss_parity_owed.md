# T16 LSS parity violation (BOSS 4PCF ~7σ claimed) — OWED
1. THE PRICING (decisive): the model's channel — helical vorticity injection (the winding + Harrison-era rotation) → parity-odd density 4-point. Compute the amplitude; the flag is honest: likely SHORT (vorticity ~ (v/c)-suppressed), but the model is nearly alone in HAVING native parity machinery at LSS scales.
2. The claim's status pass: Hou–Slepian–Cahn / Philcox 4PCF results + the systematics debate (window/covariance disputes).
3. The axis question: if real AND ours, the parity signal should correlate with the family axis — if real and NOT ours, anti-correlation expected (same structure as the dipole-excess salvage, A5).
