# Cross-cutting debts (own every thread)
1. THE LORENTZ PROGRAM (the consolidated Achilles' heel): one question owns three debts — W-W evasion pricing, the F-F applicability to preferred-frame media (internal review decisive check — where the keystone reduction can break), and the LV literature (internal review: clock-comparison/photon-dispersion/GRB, 1×10⁻¹⁴..1e-20 class). A single research file should attack all three.
2. THE α_c POSTERIOR propagates everywhere: m → α_c (the fraction), r_1s (T1), α_g (T2), ξ (BH/bounce docs), the healing-length ladder (interaction map).
3. THE STEP-DEBT AUDIT (the working docket): five coded steps, each owed its ramp reading. **Status 2026-07-16 — this is the debt that would have caught the BBN cascade, and it was left open.** The BBN weak-rate splice's ramp reading is now PAID (`scripts/prym_ramped_splice.py`): it moved Y_p, D/H, Li7, the epoch stamps, P-2026-027, P-2026-036 and the spine, and it deleted a 3.5σ "rejection" that was a step artifact. **The CMB-side transition needs NO ramp reading — `source/background.c` was ramped 2026-07-12 (tanh fade in ln(1+z), low edge) and 2026-07-14 (the growth ramp, v² ∝ 1 − T/T_c); `varying_transition_width: 0` reproduces the legacy steps and is the backward-compatible path.** **But the ramp is only ON where the config asks for it — see the live defect below.**
5. **CONFIG DEFECT, LIVE (2026-07-16): `chains/cmp_prtoe_dyad_ev.input.yaml` omits `varying_transition_width`, so it runs the legacy instantaneous-STEP path**, while its evidence companion `cmp_prtoe_fixed_ev.input.yaml` sets `0.25` and runs the ramp. It also omits `dcdf_z_rad_onset`. **The two halves of the evidence program are not running the same transition physics**, and `dyad_ev` is what the armed companion watcher (`scripts/evidence_companion_watch.sh`) launches. Any ΔlnZ comparing them compares a stepped model against a ramped one. **Not touched — the box's runs are under a standing do-not-touch; recorded so the defect is not inherited by the verdict.**
4. NO-BARE-G (the working docket): L1's deepest reading — the keystone's one remaining assumption.
