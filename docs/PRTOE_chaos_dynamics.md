# Chaos & Dynamical Systems — Natively Ours (standalone; the banked One Mountain + this era's returns)

## The equations and results
- the genesis roll: ẍ = −3H(t)ẋ − ∇V, V = m²R² + λR⁴ + 2ε_A λR⁴cos4θ (ε_A = 2/9)
- the release-angle chaos: f_amp(θ_i) spans [0.034, 0.964] with adjacent-bin jumps (def335 —
  the banked dice made quantitative); the cycle map: NO attractor (7 chains × 120 cycles,
  spreads [0.1, 0.97] to the last cycle — def361)
- the resolution: TOPOLOGY averages what chaos scatters — f̄ = ∫f_amp(θ)dθ/(π/4) = 0.635 ±
  0.026 (ramp-corrected, def407): the winding's uniform measure integrates the chaos out
- the odometer: f̄(Ri) shallow (~0.09 per 100× energy) — chaos-robust, clock-weak (def387)

## How PRTOE connects
Chaos is not decoration here — it is the model's account of WHY individual draws are lawless
(the zero-mode never settles) and WHY observables are lawful anyway (topological averaging).
The dice and the measure are the same machine viewed pointwise vs integrally.
